// Généré depuis Login.ir par ir-backend-css. Ne pas éditer : zone générée (spec §9).
import { LoginLayout } from "./Login.gen";

export default { title: "Login", component: LoginLayout };

export const Placeholders = {
  args: { subtitle: "Connectez-vous pour continuer" },
};
