// Zone préservée (spec §9) : écrite une seule fois par ir-backend-swiftui, jamais réécrite. Votre logique va ici.
import SwiftUI

struct LoginView: View {
  var body: some View {
    LoginLayout(subtitle: "Connectez-vous pour continuer")
  }
}
